unpack:
  - source: "/run/live/medium/live/filesystem.squashfs"
    sourcefs: "squashfs"
    destination: ""

# TODO: Verify the live filesystem path after the first successful ISO build.
# Some Debian live-build profiles may expose the squashfs image under a
# different path depending on boot media layout.
