# aptura-meta

`aptura-meta` is the top-level metapackage for Aptura OS.

It should depend on packages that every default Aptura desktop installation
needs. Keep optional applications in edition-specific packages later.

TODO:

- Add optional Aptura Shell applets only when they solve a real workflow problem.
- Split development, creative, and minimal editions into separate metapackages.
