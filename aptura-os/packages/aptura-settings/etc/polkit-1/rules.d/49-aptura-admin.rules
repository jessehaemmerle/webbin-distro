// Aptura OS policy namespace for future privileged desktop helpers.
// Members of sudo remain the administrative trust boundary. Keep rules narrow.
polkit.addAdminRule(function(action, subject) {
  return ["unix-group:sudo"];
});
