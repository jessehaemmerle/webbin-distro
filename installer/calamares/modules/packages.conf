# Post-install package operations. Remove live-only packages and ensure the
# Aptura kernel + desktop remain. APT is the backend (Ubuntu base).
---
backend: apt
update_db: false
update_system: false

operations:
- try_remove:
  - calamares
  - calamares-settings-debian
  - casper
  - live-boot
  - live-config
